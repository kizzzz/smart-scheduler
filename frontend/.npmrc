# 固定公共 npm registry。
#
# 若在内网镜像环境（如 bnpm）里执行 npm install，lock 里的 resolved 会被写成
# 内网地址，之后在公网服务器上 npm ci 会全量 ENOTFOUND，表现为
# "npm error Exit handler never called!"，极难定位。这里显式钉住公共源。
registry=https://registry.npmjs.org/
fund=false
audit=false
update-notifier=false
